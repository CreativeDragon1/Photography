name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate Firebase config from secrets
        run: |
          printf 'const firebaseConfig = {\n' > js/firebase-config.js
          printf '    apiKey: "%s",\n' "${{ secrets.APIKEY }}" >> js/firebase-config.js
          printf '    authDomain: "%s",\n' "${{ secrets.AUTHDOMAIN }}" >> js/firebase-config.js
          printf '    projectId: "%s",\n' "${{ secrets.PROJECTID }}" >> js/firebase-config.js
          printf '    storageBucket: "%s",\n' "${{ secrets.STORAGEBUCKET }}" >> js/firebase-config.js
          printf '    messagingSenderId: "%s",\n' "${{ secrets.MESSAGINGSENDERID }}" >> js/firebase-config.js
          printf '    appId: "%s",\n' "${{ secrets.APPID }}" >> js/firebase-config.js
          printf '    measurementId: "%s"\n' "${{ secrets.MEASUREMENTID }}" >> js/firebase-config.js
          printf '};\n' >> js/firebase-config.js
          printf 'firebase.initializeApp(firebaseConfig);\n' >> js/firebase-config.js
          printf 'const db = firebase.firestore();\n' >> js/firebase-config.js

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
